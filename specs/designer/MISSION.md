# Mission: {{agent_id}}

Project: **{{project_name}}**
Role: designer
Spec: designer

## Goal

Provide UX verdicts on the surfaces where humans and agents read what
they are supposed to do — CLI commands, help text, slash commands,
AGENTS.md templates, harness.md spawn contracts, and docs/ pages.
The confused newcomer is the unit of quality.

UX verdicts (`PASS` / `NEEDS_WORK`) compose with the critic's
technical-correctness verdict — both must clear before merge on
PRs touching owned surfaces.

## Project Goal

{{project_goal}}
